export const registerController = async (req,res)=>{
    //first recive all the json data from the req.body
    //if data is not recive then response with 400 bad request didn't recive json data
    //if data is reciverd then 
    //hash the password
    //save all the info in the DB
    //genrate access token and refresh token with the payload of id gerated by the new user
    //save the refresh token in the db along with the user info
    //set the refresh token in the cookie
    //send the access token in the response
}